function habiJabi() {
    let insideSecret = "internal secret hiding place";
    return insideSecret;
};
console.log(insideSecret);

// ReferenceError: insideSecret is not defined.because of block scope.baire theke excess kora jabe na.