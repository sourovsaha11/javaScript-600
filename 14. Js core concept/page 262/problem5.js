const student = {
    name: "serious sojib",
    roll: 1,
    masks: 99
};
console.log(student.marks);

// here we don't have marks key and value . because the key are case sensitive and we make a mistake there .thats why we can see undefined in our console panel.