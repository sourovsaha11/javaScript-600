const arr = [];
const newVariable = arr;
if (arr === newVariable) {
    console.log("same"); 
}
else {
    console.log("not same");
}
// same because reference same duitar .eki memory location both variable er.