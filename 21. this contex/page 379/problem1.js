/*
The window object is the global object in browsers that represents the browser window or tab. It provides properties, methods, and events for controlling and interacting with the browser.

All global variables and functions declared in JavaScript become properties of the window object.

*/