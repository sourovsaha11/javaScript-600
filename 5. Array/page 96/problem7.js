const tableMaterials = ['pen', 'board', 'keyboard', 'books'];
const seventhItem = tableMaterials[7];
console.log(seventhItem);

// the answer will be undefined because there is no 7th element in this array.