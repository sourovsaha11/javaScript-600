const product = {
    price: 999,
    quantity: 1
};
function totalPriceProduct(product) {
    // if (typeof product.price !== 'number' && typeof product.quantity !== "number"); {
    // }
    price = 10;
    quantity = 1;
    totalPrice = price * quantity;
    return totalPrice;
}
const motDam = totalPriceProduct(product);
console.log(motDam);