const noteBook = "JavaScript";

console.log(noteBook == 'JavaScript');
console.log(noteBook === 'JavaScript');

// both are string data type . and there value is also same . thats why both are true. and we can use '' or "" .... both are same . but you can not use both at a same time . you have to use one of them .