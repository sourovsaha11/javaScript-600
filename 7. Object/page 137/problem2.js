const article = {
    title: 'Learning JS',
    category: 'programming'
};
const profileKeys = Object.keys(article);
const hasProperty = profileKeys.includes("author");
console.log(hasProperty);

//includes boolean value return kore , r eta case sensitive , and bracket er vitore obossoi quotation mark dite hobe. 